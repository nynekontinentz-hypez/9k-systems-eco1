fastapi>=0.115
uvicorn[standard]>=0.30
requests>=2.32
# Pin the full Piper dependency set — its resolver is fragile; explicit pins keep
# the cp311 manylinux wheels deterministic on the build host.
onnxruntime==1.16.3
piper-phonemize==1.1.0
piper-tts==1.2.0
